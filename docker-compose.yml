services:
  ihopecash:
    build: .
    container_name: ihopecash
    restart: unless-stopped
    ports:
      - "443:443"
    volumes:
      - ./data:/app/data
      - ./env.yaml:/app/env.yaml
      - ./certs:/app/certs
    deploy:
      resources:
        limits:
          memory: 1g
          cpus: "2.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
